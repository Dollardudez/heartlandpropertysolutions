/**
 * Building a React Context just allows us to send state between components
 * without having to send props through each component. The context, then, will
 * be set in the highest order component.
 */